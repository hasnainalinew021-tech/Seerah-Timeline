import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF0D9488);
  static const Color primaryLight = Color(0xFF2CC5BC);
  static const Color secondary = accent;

  // Background colors
  static const Color scaffoldBackground = Color(0xFFF2F8F5);
  static const Color backgroundLight = Color(0xFFE8F5F3);
  static const Color backgroundMint = Color(0xFFD4F1ED);
  static const Color white = Colors.white;

  // Accent colorsks
  static const Color accent = Color(0xFFFEA82F);
  static const Color accentLight = Color(0xFFFBBF24);

  // Text colors
  static const Color textPrimary = Color(0xFF1F2937);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textLight = Color(0xFF9CA3AF);

  // Icon colors
  static const Color iconPrimary = primary;
  static const Color iconSecondary = Color(0xFF64748B);

  // Status colors
  static const Color success = Color(0xFF10B981);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
  static const Color info = Color(0xFF3B82F6);

  // Card colors
  static const Color cardBackground = Colors.white;
}
